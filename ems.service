[Unit]
Description=EMS Supervisord
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/supervisord -c /home/pi/ems/supervisord.conf
Restart=always

[Install]
WantedBy=multi-user.target
